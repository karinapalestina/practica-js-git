
let promedio = 9.5;
let materiasReprobadas = 0;
let esDeportista = false;

// Expresión lógica
let obtieneBeca = (promedio >= 9.0 && materiasReprobadas === 0) || esDeportista;

// Mostrar resultado
console.log(`¿Obtiene beca?: ${obtieneBeca}`);

